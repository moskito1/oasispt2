CREATE TABLE record
  (id BIGINT(50) NOT NULL,
  Item_Name VARCHAR(255) NOT NULL,
  size_oz VARCHAR(255) NOT NULL,
  price DOUBLE(10,2) NOT NULL,
  quantity BIGINT(50) NOT NULL,
  total_price DOUBLE(10,2)
  );
